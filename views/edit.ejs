<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit The Message</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>

    <h3>Edit Your Chat Id:<%=chat._id%></h3>
    <p name="chat">This chat was sent from <b><%=chat.from%></b> to <b><%=chat.to%></b></p>

    
    <form method="POST" action="/chats/<%= chat._id %>?_method=PUT">
        <textarea name="msg" rows="10" cols="50" placeholder="Type Message Here"></textarea>
        <button type="submit">Save Edit</button>
    </form>
    
</body>
</html>